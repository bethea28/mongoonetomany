import React, { useState } from 'react';

export default function App() {
  const availableBreeds = []; // TODO: Populate this!

  return (
    <div className="App">
      <p>I have { availableBreeds.length } breeds ready to be searched!</p>
    </div>
  );
}
